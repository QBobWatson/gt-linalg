<?xml version="1.0" encoding="UTF-8"?>
<!--********************************************************************
Copyright 2017 Georgia Institute of Technology

Permission is granted to copy, distribute and/or modify this document
under the terms of the GNU Free Documentation License, Version 1.3 or
any later version published by the Free Software Foundation.  A copy of
the license is included in gfdl.xml.
*********************************************************************-->
<exercises>
    <exercise>
        <statement>
            <p>Let <m>A</m> be a symmetric matrix. Explain step by step how to find an orthonormal basis of eigenvectors of <m>A</m>. (Be sure to consider the case when not all eigenvalues of <m>A</m> are distinct).</p>
        </statement>
    </exercise>
    <exercise>
        <statement>
            <p>Let <m>A</m> be a symmetric matrix. Explain step by step how to find an matrix <m>U</m> and a diagonal matrix <m>D</m> such that <m>A = UDU^T</m> and <m>U^T = U\inv</m>.</p>
        </statement>
    </exercise>
    <exercise>
        <statement>
            <p>Let <m>A</m> be a symmetric matrix. Explain step by step how to find a spectral decomposition of <m>A</m>.</p>
        </statement>
    </exercise>
    <exercise>
        <statement>
            <p>Let <m>A</m> be a symmetric matrix. If <m>\lambda</m> is a root of <m>\det(A-\lambda I)</m> of multiplicity <m>k</m> , explain why the null space of <m>A-\lambda I</m> has dimension <m>k</m>.</p>
        </statement>
    </exercise>
    <exercise>
        <statement>
            <p>Let <m>A</m> be a symmetric matrix with eigenvalues <m>\lambda_1, \lambda_2</m> and the corresponding eigenvectors <m>v1, v2</m>. Assume <m>\lambda_1 = \lambda_2</m>. Explain why <m>v1, v2</m> are orthogonal.</p>
        </statement>
    </exercise>
    <exercise>
        <statement>
            <p>Let <m>Q: \R^n\to\R</m> be a quadratic form, that is, a function of the form <m>\sum_{i, j=1}^n a_{ij} x_i x_j</m> where <m>a_{ij}</m> are real numbers and <m>x_k</m> is the <m>k</m>th coordinate on <m>\R^n</m>. Explain how to find a symmetric <m>n \times n</m> matrix <m>A</m> such that <m>Q(x)=Ax\cdot x</m> for all <m>x</m> in <m>\R^n</m>.</p>
        </statement>
    </exercise>
    <exercise>
        <statement>
            <p>Let <m>Q(x) = Ax\cdot x</m> where <m>A</m> is a symmetric <m>n \times n</m> matrix with eigenvalues <m>\lambda_1, \ldots , \lambda_n</m>. Explain how to find a change of variables so that <m>Q</m> becomes <m>\lambda_1 {y_1}^2 + \cdots + \lambda_n {y_n}^2</m>).</p>
        </statement>
    </exercise>
    <exercise>
        <statement>
            <p>Let <m>Q(x) = Ax\cdot x</m> where <m>A</m> is a symmetric <m>n \times n</m> matrix. Explain why <q><m>Q(x) > 0</m> for every <m>x \neq 0</m> if and only if every eigenvalue of <m>A</m> is positive</q>.</p>
        </statement>
    </exercise>
    <exercise>
        <statement>
            <p>Let <m>Q(x) = Ax\cdot x</m> where <m>A</m> is a symmetric <m>n \times n</m> matrix. Explain why <q><m>Q(x) \geq 0</m> for every <m>x \neq 0</m> if and only if every eigenvalue of <m>A</m> is nonnegative</q>.</p>
        </statement>
    </exercise>
    <exercise>
        <statement>
            <p>Let <m>Q(x) = Ax\cdot x</m> where <m>A</m> is a symmetric <m>n \times n</m> matrix with eigenvalues <m>\lambda_1, \ldots , \lambda_n</m> and an orthonormal basis of eigenvectors <m>u_1,\ldots, u_n</m>, where <m>Au_i=\lambda_i u_i</m>. What are the maximum and the minimum of the function <m>Q(x)</m>Q(x) under the constrain <m>\|x\|=1</m>? For which <m>x</m> are the the maximum and the minimum attained?</p>
        </statement>
    </exercise>
    <exercise>
        <statement>
            <p>Let <m>A</m> be a symmetric <m>n \times n</m> such that <m>Q_A(x) = Ax\cdot x</m> is positive definite. Explain why the matrix <m>A\inv</m> is invertible and symmetric. Explain why the quadratic form <m>Q_{A\inv}(x) = A\inv x\cdot x</m> is positive definite.</p>
        </statement>
    </exercise>
    <exercise>
        <statement>
            <p>If <m>A</m> and <m>B</m> are symmetric matrices whose eigenvalues are positive, explain why the eigenvalues of <m>A+B</m> are positive.</p>
        </statement>
    </exercise>
    <exercise>
    	<statement>
    		<p>Suppose <m>u</m> is a unit vector in <m>\R^n</m>, so <m>u^T u=1</m>. This problem is about <m>n \times n</m> symmetric matrix <m>H = I - 2uu^T</m>.
    			<ol>
    				<li>Show directly that <m>H^2 = I</m>. Since <m>H=H^T</m>, we now know that <m>H</m> is not only symmetric but also <fillin/>.</li>
    				<li>One eigenvector for <m>H</m> is <m>u</m> itself. Find the corresponding eigenvalue.</li>
    				<li>If <m>v</m> is any vector perpendicular to <m>u</m>, show that <m>v</m> is an eigenvector of <m>H</m> and find the eigenvalue. With all these eigenvectors <m>v</m>, that eigenvalue must be repeated how many times? Is <m>H</m> diagonalizable? Why or Why not?</li>
    				<li>Find the diagonal entries <m>H_{11}</m> and <m>H_{ii}</m> in terms of <m>u_1 \ldots u_n</m>. Add up <m>H_{11} + \ldots + H_{nn}</m> and separately add up the eigenvalues of <m>H</m>.</li>
    			</ol>
    		</p>
    	</statement>
    </exercise>
</exercises>
